// 'use strict';

// var gulp = require('gulp'),
//     runSequence = require('run-sequence');

// function build(callback) {
//     gulp.series('clean', 'images', 'scripts', 'styles', 'node_modules', 'runStatic', callback);
// };

// exports.default = build;
